# 成本口径

`CostBreakdown` 为回测结果提供统一的费用视图：

| 字段 | 含义 |
|------|------|
| `fee_cost` | 显式费用 |
| `slippage_cost` | 隐式滑点 |
| `total_cost` | 两者之和 |

后续新增佣金、税费、价差、市场冲击或机会成本时，应继续保持分项字段，避免把全部成本压缩成一个无法审计的 bps 数字。
